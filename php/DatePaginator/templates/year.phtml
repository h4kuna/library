<div class="vdp">
{foreach $dp as $date}
	{if $date->isCurrent()}
		{!$date->view}
	{else}
	<a href="{link this, date=>$date->url}">{!$date->view}</a>
	{/if}
	{if !$iterator->isLast()} | {/if}
{/foreach}
</div>